<div class="admin-content">
	<%= link_to "Add post", new_admin_post_path, class: "add-type" %>
	<table>
		<tbody>
			<% @posts.each do |post| %>
				<tr><td><%= link_to "×", admin_post_path(post), :confirm => "Are you sure you want to delete #{post.title}?", :method => :delete, :class => "delete" %><%= link_to post.title, edit_admin_post_path(post) %></td></tr>
			<% end %>
		</tbody>
	</table>
</div>



