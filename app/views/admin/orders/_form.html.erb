<%= semantic_form_for [:admin, @order], :html => {:multipart => true} do |f| %>
	<%= link_to "Go to product", edit_admin_product_path(@order.product_id) %>
  <%= f.inputs :name%>
  <%= f.inputs :phone%>
  <%= f.inputs :email%>
  <%= f.actions%>
<% end %>
