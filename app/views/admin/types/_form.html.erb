<%= semantic_form_for [:admin, @type], :html => {:multipart => true} do |f| %>
  <%= f.inputs do%>
    <%= f.input :name %>
    <%= f.input :alias, :as => :string %>
  <% end %>
  
  <%= f.actions do %>
    <%= f.action :submit %>
  <% end %>
<% end %>
