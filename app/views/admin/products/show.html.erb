<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @product.name %>
  <% @product.attached_images.each do |photo| %>
  	<%= image_tag photo.image.url(:thumb5) %>
  <% end %>
</p>


<%= link_to 'Edit', edit_admin_product_path(@product) %> |
<%= link_to 'Back', admin_products_path %>
